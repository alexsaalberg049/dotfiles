set nocompatible

syntax on
set tabstop=4
set shiftwidth=4

set linebreak
"If on Vim will wrap long lines at a character in 'breakat' rather than at the last character that fits on the screen.
"'breakat' 'brk' string (default \" ^I!@*-+;:,./?")

set number

set showmatch "Parenthesis' highlighting

set pastetoggle=<F9> "press f9 to go to paste mode

set autoindent

set hlsearch "highlight matches

"Have buffers in background. Let's u switch buffers without saving
set hidden 

"Mouse scrolling and clicking
set mouse=a

" let's you put th ecursor onto the 'empty' space at the end of every line
set ve+=onemore

set clipboard=unnamed
